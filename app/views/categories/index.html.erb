<% provide(:title, t("categories")) %>
<h1><%= t("categories-title") %></h1>

<%= will_paginate %>

<ul class="categories">
  <% @categories.each do |category| %>
    <li>
      <%= category.name %>
      <%= link_to t("start-categories"), category_lessons_path(category.id), method: :post, class: "btn btn-primary" %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
